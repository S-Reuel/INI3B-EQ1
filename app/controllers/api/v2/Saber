Converti método GET para POST, funciona de forma perfeita!!!
Front não está conseguindo consumir dados dele mesmo, o que é um problema.
Realiza-se uma consulta que consegue o id de equipe, e envia isso para projeto, porém isso é feito através de um GET, deve ser trocado para POST, não acho que seja algo tão complexo, veremos...


SEMPRE QUE INICIAR O SERVIDOR
trocar o link do ngrok em "task_serializer.rb" e "usuario_serializer" para o nome_novo
atualize as imagens

!!!Quando for subir para o servidor real, reconfigurar a rota

Excluir campo arquivos de Tasks
Criar tabela arquivos

**CÓDIGOS**

!Renomear nome de arquivo (Rails)!

>> File.rename(nome antigo, nome_novo)

!Salvar arquivo em uma pasta!

# 'public' é a pasta

>> file_path = Rails.root.join('public', 'pasta', document.filename)

# vê se a pasta existe
>> FileUtils.mkdir_p(File.dirname(file_path))

# Salva o arquivo
>> File.open(file_path, 'wb') do |file|
  >> file.write(document.read)
>> end